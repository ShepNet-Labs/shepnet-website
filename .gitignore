node_modules/
npm-debug.log*
.DS_Store
Thumbs.db
*.local

# Internal business data must NEVER be committed (pricing, labor, financials).
/internal/
*.pricing.*
*-internal.*

# Local Claude Code config (never commit; exposes local paths)
.claude/
.claude/settings.local.json

{
  "name": "shepnet-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for ShepNet Security, built with Tailwind CSS (production build, no CDN).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./css/styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./css/styles.css --watch",
    "preview": "python3 -m http.server 8080",
    "dev": "npm run build && npm run preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
